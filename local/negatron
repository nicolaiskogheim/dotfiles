
# If not running interactively, don't do anything
[[ $- != *i* ]] && return

# yaourt wants to know my editor
export VISUAL=vim

# alias ls='ls --color=auto'
# PS1='[\u@\h \W]\$ '
AUTOENV_PATH="/usr/share/autoenv-git/activate.sh"
test -f "$AUTOENV_PATH" && source "$AUTOENV_PATH"

